// Default Page sizes
export const DEFAULT_PAGE_SIZES = [10, 20, 50, 100];

// all languages
export const ALL_LANGUAGES = {
  English: "en-US",
  Thai: "th",
};

// all roles
export const ALL_ROLES = {
  ADMIN: "ADMIN",
};
